# Driver Creator Agent - Environment Variables

# Anthropic API Key (required)
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (required for mem0 embeddings)
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# E2B API Key (required for testing generated drivers)
# Get yours at: https://e2b.dev/
E2B_API_KEY=your_e2b_api_key_here

# Claude Model Selection
# Options: claude-sonnet-4-5, claude-sonnet-4, claude-haiku-4-5
# Default: claude-sonnet-4-5 (best for complex tasks)
CLAUDE_MODEL=claude-sonnet-4-5

# Server Configuration
HOST=0.0.0.0
PORT=8080

# Debug Mode
DEBUG=false

# Self-Healing Agent System Configuration
# Layer 3 (Supervisor): Max attempts at supervisor level
# Each supervisor attempt runs the full generation pipeline
MAX_SUPERVISOR_ATTEMPTS=3

# Layer 2 (Fix-Retry): Max fix-retry iterations per supervisor attempt
# How many times to try fixing and retrying within one supervisor attempt
MAX_FIX_RETRY_ITERATIONS=7

# Layer 1 (Defensive): Max retries per file generation
# How many times to retry generating a single file with validation feedback
MAX_FILE_GENERATION_RETRIES=3

# Note: Total possible attempts = MAX_SUPERVISOR_ATTEMPTS × MAX_FIX_RETRY_ITERATIONS
# Example: 3 × 7 = up to 21 total generation attempts
# Recommended for testing: Increase MAX_SUPERVISOR_ATTEMPTS, reduce MAX_FIX_RETRY_ITERATIONS
